<div class="container">
    
    <h1>{{article.title}}</h1>


    <img src="{{article.img}}" alt="{{article.title}}">

    <p>{{article.text}}</p>

    <a class="btn btn-primary" href="/article">Назад</a>

</div>